{
    "name": "voicehu-root",
    "version": "1.0.0",
    "description": "VoiceHU - Academic Feedback & Complaint Intelligence Platform",
    "scripts": {
        "install-all": "npm install && cd server && npm install && cd ../client && npm install",
        "seed": "cd server && npm run seed",
        "server": "cd server && npm run dev",
        "client": "cd client && npm start",
        "dev": "concurrently \"npm run server\" \"npm run client\"",
        "build": "npm install && cd server && npm install && cd ../client && npm install && npm run build",
        "prod": "NODE_ENV=production node server/server.js",
        "deploy": "npm run build && npm run seed && npm run prod",
        "start": "node server/server.js"
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}